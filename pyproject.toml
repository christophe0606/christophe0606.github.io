[project]
name = "lean-favergeon-blog-tools"
version = "0.1.0"
description = "Manual image preparation for the Verso blog"
requires-python = ">=3.12"
dependencies = ["pillow>=12,<13"]

[tool.uv]
package = false
